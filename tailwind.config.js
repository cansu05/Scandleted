/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      colors: {
        background: "#F9F5EA", // Ana arka plan rengi
        secondary: "#ECDFDB", // İkinci arka plan rengi
        border: "#C9A489", // Genel border, buton border rengi
        heading: "#C9A489", // Başlıklarda span olarak kullanılan renk
        footer: "#0C0D12", // Footer arka planı ve genel yazı rengi
        text: "#555659", // Paragraflar için metin rengi
      },
    },
  },
  plugins: [],
};
